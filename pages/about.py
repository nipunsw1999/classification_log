import streamlit as st

st.title("About")

st.write("This project uses a synthetic dataset, where :red[all the data is not real]. However, the techniques applied here :green[can be adapted] for real-world applications in similar contexts.")